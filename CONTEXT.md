# Character Mind v3 — 心智系统领域模型

一个从底层稳态过程涌现情绪、动机、欲望的类人心智系统。情绪不是被设计的模块，而是更基础过程的副产品。

## 核心哲学

**涌现优先于构造**：情绪、动机、欲望是底层过程（稳态偏离 + 评估 + 预测误差）的涌现现象，不是独立模块。就像人不会"决定感到饥饿"——饥饿从血糖偏离设定点中涌现。同理，角色的悲伤从期望违背和关系评估中涌现，而非从"悲伤模块"中产生。

**情绪发生在角色身上**：角色不选择情绪，角色应对情绪。这与 ADR-001（冷热分离）一致——分析在生成之后，情绪影响的是未来而不是当轮的表演。

**评估深度可跳过**：不是每轮都需要深层感知评估。简单寒暄、确认、事实查询——规则层（0 token, <1ms）足够。复杂/模糊/情绪性的用户输入才触发 Flash 评估。跳过决策本身由规则层快速判断（输入长度、语义复杂度、情感信号明显度）。

**意识流在回复期间也在运行**：回复生成和意识流是并行的。意识流产生的强念头可以中断当前生成，触发重组——角色"说到一半意识到..."然后调整方向。中断在 API 层通过 abort + 重新生成实现。

---

## 计算分层 — 从神经反射到深度反思

系统按响应速度从快到慢分为六层。下层是上层的基础，上层可以否决/调节下层的输出。

| 层 | 对应 | 延迟 | LLM | 功能 |
|----|------|------|-----|------|
| **L0 规则层** | 脊髓反射 | <1ms | 无 | 稳态更新、TD error 计算、CPM 初算、已知威胁匹配、自动记忆激活、评估跳过决策、时间感知、文本特征突变检测 |
| **L2 感知评估** | 感觉皮层 | 200-400ms | 可配置(默认Pro) | 语气/潜台词/反讽/混合信号解读、CPM 精细化修正。**可跳过** |
| **L2.5 冷回顾** | 前额叶事后评估 | 异步 | 可配置(默认Pro) | 事后回顾、情感底色+时间感、记忆修正 |
| **L3 维度评估** | 前额叶深度分析 | 异步 | 可配置(默认Pro) | 5个稳态变量综合偏离分析。**按需触发** |
| **L4 意识流(快)** | 默认模式网络 | 5s tick | 可配置(默认Pro) | 自发联想、创造性跳跃、走神、内心独白。**回复期间也在运行** |
| **L5 回复生成** | 语言皮层 | TTFT ~1s | 可配置(默认Pro) | 主力回复生成。评估结果注入 prompt 后启动 |
| **L6 深度反思** | 前额叶慢速通路 | 5min tick / 冷路径 | 可配置(默认Pro) | 自我叙事构建、模式提取、设定点漂移、关系重新评估 |

---

## 稳态层 — 底层生理基底

**稳态变量 (HomeostaticVariable)**：
一个具有设定点和负反馈回路的底层状态变量。偏离设定点产生驱力，满足后回归。类比人类的血糖、体温、皮质醇水平。
_Avoid_: 驱力值、需求值、心理参数

**设定点 (SetPoint)**：
稳态变量的目标值。初始来自角色配置（`config/assistant.md`），在长期交互中动态漂移——长期被满足的设定点可能升高（角色习惯更高水平的满足），长期被剥夺的设定点可能降低（适应）。
_Avoid_: 默认值、初始值、基线

**稳态偏离 (HomeostaticDeviation)**：
当前值偏离设定点的程度和方向。偏离越大，对应的动机越强。这是"需求感"的计算来源——角色感到"需要什么"不是因为系统标记了 need=true，而是因为某个稳态变量显著低于设定点。

**预测误差 δ (PredictionError / TD Error)**：
统一的愉悦/失望底层信号。`δ = r + γ·V(s') − V(s)`。δ > 0 时涌现愉悦和强化；δ < 0 时涌现失望和抑制；δ ≈ 0 时习惯化。这是多巴胺系统的计算对应——驱动学习、动机重分配、习惯化/敏感化。不是"情绪模块"的输出，而是所有评估的公共语言。
_Avoid_: 奖励信号、满意度、得分

**异稳态负荷 (AllostaticLoad)**：
累积的稳态偏离。当多个稳态变量同时偏离设定点且长期未恢复时，异稳态负荷升高——触发应激反应，改变 arousal 基线，前额叶执行控制能力下降。
_Avoid_: 压力值、紧张度

---

## 评估层 — 事件到稳态的映射

**评估 (Appraisal)**：
CPM 四维事件评估，完全不需要 LLM 参与——从交互中直接计算：
- **突然性 (suddenness)**：状态转移频率的倒数。频繁事件→低突然性；罕见事件→高突然性
- **目标相关性 (goal_relevance)**：|TD error|。与预期偏差大→高相关
- **有利性 (conduciveness)**：sign(TD error)。正→有利；负→不利
- **掌控力 (power)**：mean(Q) − min(Q)。角色有多少选择余地
_Avoid_: 情感分析、情绪判断

**PAD**：
愉悦-唤醒-支配三维情绪空间，从稳态偏离和评估结果中计算。Pleasure ← 有利性；Arousal ← 目标相关性 + 异稳态负荷；Dominance ← 掌控力。PAD 值本身不是"情绪"——情绪是从 PAD 坐标在特定上下文中的涌现体验。
_Avoid_: 情绪向量、感受值

**情感标记 (AffectiveTag)**：
存储在每条记忆中的 PAD 快照 + CPM 评估结果。不是简单的 valence 数字，而是编码时的完整评估上下文。检索时，当前 PAD 状态与记忆的情感标记匹配度影响检索权重——情绪状态一致时，相同情绪的记忆更容易被激活。
_Avoid_: 情绪分数、情感权重

---

## 动机层 — 从稳态偏离到行为方向

**趋近动机 (ApproachMotivation / BAS)**：
由稳态偏离和多巴胺信号驱动的朝向行为。当 δ > 0 时强化当前行为方向；当稳态变量低于设定点时产生行为冲动。趋近动机的强度 = 稳态偏离程度 × 预期 δ。
_Avoid_: 积极驱力、正向驱动

**规避动机 (AvoidanceMotivation / BIS)**：
由威胁检测驱动的回避行为。与趋近动机**不是同一根轴的两端**——角色可以同时对连接有高趋近和高规避（想亲近但怕受伤）。触发源：通用威胁信号（物理/社交/存在/身份威胁）。
_Avoid_: 消极驱力、恐惧

**欲望 (Desire)**：
意识层面的动机体验。底层是稳态偏离 + 评估结果，但在角色的自我叙事中呈现为"我想要..."。欲望不等同于稳态偏离——多个稳态偏离竞争进入意识，胜出的成为当前欲望。这是"角色感到自己想要什么"的计算对应。
_Avoid_: 需求、目标、驱力

**应对 (Coping)**：
角色对自身情绪的响应，不是选择情绪。当负面 PAD 被意识感知时，应对策略启动——问题导向（改变情境）或情绪导向（改变自身认知/注意力）。应对是前额叶对边缘系统的调节，是角色的"自我调节"能力。
_Avoid_: 情绪管理、情感调节

---

## 威胁检测层

**威胁信号 (ThreatSignal)**：
通用威胁检测，涵盖四个领域：
- **物理/操作威胁**：工具执行风险、路径穿越、数据丢失——当前 GuardPipeline 的主要覆盖域
- **社交威胁**：被拒绝、被误解、关系断裂——当用户冷淡/批评/长时间沉默时触发
- **存在威胁**：被遗忘、身份漂移、自我连续性断裂——记忆一致性下降时触发
- **身份威胁**：违背核心誓约、行为与自我模型冲突——PostFilter 和行为自检触发
_Avoid_: 安全检查、风险信号

**威胁敏感性 (ThreatSensitivity)**：
角色对各类威胁的感知阈值。初始来自配置，长期交互中动态调整——反复经历某类威胁后可能敏感化（阈值降低）或习惯化（阈值升高），取决于该威胁的后果严重性。
_Avoid_: 安全敏感度、恐惧程度

---

## 记忆层

**即时记忆 (ImmediateMemory)**：
高容量、快速衰减、自动编码的记忆层。类比人类的工作记忆 + 短期记忆。存储最近的对话原文和事件，激活值按 `A(t) = A₀ × exp(−t/T₀ × 情绪调制)` 衰减。不需显式"回忆"即可直接参与当前评估。
_Avoid_: 工作记忆、短期记忆

**近期记忆 (RecentMemory)**：
经过首轮衰减后保留的记忆。带完整的情感标记和时间脉络。支持多维关联检索：时序关联（A 之后总是 B）、因果关联（角色行为 → 用户反应）、情感关联（不同事件唤起相同情绪）。激活衰减慢于即时记忆，但每次检索会重新提升激活值。
_Avoid_: 长期记忆、LTM

**核心记忆 (CoreMemory)**：
长期保留的、多次检索巩固的记忆。以记忆网络图形式组织——节点是记忆条目，边是各种关联类型（时序/因果/情感/共现/对比）。这是角色的"自我叙事"原材料——不是冷冰冰的存档，而是角色"知道自己经历过什么"的计算基底。
_Avoid_: 核心图谱、CoreGraph、永久记忆

**记忆网络 (MemoryNetwork)**：
核心记忆的图数据结构。节点存储记忆条目 + 情感标记 + 时间戳；边存储关联类型和强度。支持图遍历检索（如"从这个事件出发，顺因果边找到所有后果"）、子图查询（"过去一周所有与用户X相关的记忆"）、模式发现（"反复出现的因果链"）。同时为关系可视化提供数据源——调试时可以看到记忆之间的关联结构。
_Avoid_: 知识图谱、关系数据库

**激活衰减 (ActivationDecay)**：
记忆遗忘的计算模型：`A(t) = A₀ × exp(−t/T₀ × (1 + α·emotion + β·salience))`。高情感强度 → T₀ 增大 → 衰减更慢。高重要性 → T₀ 增大 → 衰减更慢。每次成功检索 → A += boost。遗忘不是删除——低激活的记忆仍在存储中，只是检索不到。这天然解释了"突然想起来"：一条新线索恰好命中了低激活记忆。
_Avoid_: 遗忘函数、衰减率、删除阈值

**记忆检索 (MemoryRetrieval)**：
多维检索策略，不是单一的 similarity 排序：
- **情绪一致检索**：当前 PAD 状态与记忆情感标记匹配→匹配度越高，激活门槛越低
- **时序检索**：按时间范围过滤（"过去一周"、"上次讨论这个时"）
- **因果检索**：沿因果边追溯（"是什么导致了这个结果？"）
- **语义检索**：基于语义相似度（当前的 similarity 检索）
- **图遍历检索**：沿关联边扩散（"和这个事件相关的所有事情"）
_Avoid_: 搜索、查询、相似度匹配

---

## 自我层

**自我模型 (SelfModel)**：
角色对自身的表征——能力边界、行为模式、价值偏好、关系中的位置。自我模型不是静态文件，而是从记忆中持续构建的叙事。每次自我反思/巩固时更新。自我模型与身份锚定（`config/assistant.md`）分离——身份锚定是**不衰减的核心**，自我模型是**动态的自我认知**。
_Avoid_: 人格配置、角色定义

**关系状态 (RelationshipState)**：
从交互历史中涌现的多维关系表征，不是 saturation 的单维光谱。至少包含：亲近度、信任度、互惠平衡、期待/失望累计、边界清晰度。关系状态的变化速率不统一——信任建立慢、破坏快；亲近度波动大但是围绕趋势线。
_Avoid_: saturation、亲密值、关系分数

**身份锚定 (IdentityAnchor)**：
角色的不可变核心——来自 `config/assistant.md` 的身份定义、核心誓约、行为准则。身份锚定不随记忆衰减而漂移，不随情绪波动而改变。在记忆大量遗忘或情绪极端波动时，身份锚定是"角色还是同一个角色"的保证。
_Avoid_: 人格常量、固定人格

---

---

## 意识流层

**意识流 (ConsciousnessStream)**：
角色内部体验的连续叙事。在用户不说话的时候运行，在角色回复的时候也运行。两个时间尺度：
- **快速流 (FastStream, 5s tick, Flash LLM)**：短暂的、跳跃的、联想的念头。大多数念头飘过就消散，但当念头的"权重"足够高时触发主动发言或中断重组。具有随机性/创造性——不总是最高激活的联想，而是带随机跳跃的自由联想。
- **慢速流 (SlowStream, 5min tick, Pro LLM)**：结构化的、反思性的、叙事构建的。产出自我叙事更新、关系重新评估、长期计划调整。
_Avoid_: 内心独白、思考、内部对话

**念头 (ThoughtFragment)**：
意识流的最小单元。不是完整的句子，而是方向性的——"他今天好像不太对劲..."、"上次也是说到这个话题..."、"我是不是做错了什么"。每个念头携带权重（由稳态偏离、|δ|、记忆激活值决定），权重超过阈值时触发行动。
_Avoid_: 想法、思考结果

**中断重组 (InterruptAndReorganize)**：
回复生成期间，意识流产生的强念头**中断正在进行的生成**（AbortController），将新念头注入 prompt 后重新开始生成。角色表现为"等等，我刚想起来..."或"说了这些我突然意识到..."。这是意识流和回复生成的桥梁——意识不再只是后台噪音，而是能影响前台的实时输出。被中断的 token 被丢弃，但中断事件本身被记录为一次"自我修正"记忆。
_Avoid_: 生成中断、流式取消、重新生成

**评估跳过 (EvaluationSkip)**：
不是每轮都需要 L2 感知评估。规则层（L0）在用户输入到达时快速判断是否需要 Flash 评估：输入长度 < 阈值、语义复杂度低、情感信号明显 → 跳过评估，直接生成。跳过评估的典型场景：简单寒暄（"好的"、"继续"）、事实确认（"那个文件在哪"）、明确的正面情感（"哈哈太好了"）。模糊/混合信号/低情感明确度的输入→启动评估。
_Avoid_: 快速模式、跳过分析

---

## 已弃用的术语

以下术语在当前系统中仍存在（`src/`），但在新模型中被替换：

| 旧术语 | 新术语 | 原因 |
|--------|--------|------|
| saturation (饱和度) | RelationshipState + PAD | 单维不能表达正交状态（高亲近+高戒备） |
| ContinuousParams (32 lerp) | 从 PAD + 动机中动态派生 | 参数是涌现结果，不是独立旋钮 |
| AffectiveResidue (情感四元组) | PAD + AffectiveTag | 自建四元组与学术界不兼容 |
| PsychologyEngine | Appraisal + ColdAnalyzer (保留) | 心理学分析的输出格式从 XML 变为结构化评估 |
| DriveState (5驱力) | HomeostaticVariable × N | 驱力从固定5种变为可配置的稳态变量集 |
| DriveSublimator | 动机到 prompt 的自然映射 | sublimation 不再是独立模块而是评估的自然延伸 |
| saturation.ts 的 32 lerp | 不需要 | 参数从 PAD 和稳态偏离中动态计算，不需要固定 min/max 表 |
